class Solution:
    def climbStairs(self, n: int) -> int:
        dp = [0] * (n + 1)
        for i in range(n+1):
            for j in range(1,3):
                if j <= i:
                    dp[i] = dp[i] + dp[i-j]
        
        return dp[n]
