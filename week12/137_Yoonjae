class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        dic = defaultdict(int)
        for n in nums:
            dic[n] += 1
        for key, val in dic.items():
            if val == 1:
                return key
        return -1